{
  "_comment": "Optional FileSecretStore. Map a server's secretKey -> password. Only needed when a server in servers.json uses 'secretKey' instead of an inline plaintext 'password'. File mode 0600 is enforced on write.",
  "oepas1-secret": "your-password-here"
}
