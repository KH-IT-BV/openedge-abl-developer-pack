# APM build output (generated by `apm install` / `apm compile` / `apm pack`) — never commit
apm_modules/
build/
.claude/
.claude-plugin/
.github/
.cursor/
.codex/

# Keep apm.lock.yaml committed (pins resolved sources + hashes) — do NOT ignore it.
